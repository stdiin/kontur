[package]
name = "kontur"
version = "0.1.0"
edition = "2024"

[dependencies]
egui-macroquad = "0.17.3"
rkyv = "0.8.18"

[target.'cfg(not(target_os = "android"))'.dependencies]
rfd = "0.17.2"

[package.metadata.android]
assets = "assets"

[profile.release]
opt-level = "z"
lto = true
strip = true
